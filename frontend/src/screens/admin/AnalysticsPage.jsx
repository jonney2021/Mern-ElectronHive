// import { BASE_URL } from "../../constants";
import AnalyticsPageComponent from "./components/AnalyticsPageComponent";
// import io from "socket.io-client";

// const ENDPOINT = "http://localhost:5000";

const AdminAnalyticsPage = () => {
  // const socket = io("", { transports: ["websocket"] }); // Create the socket instance here
  // return <AnalyticsPageComponent socketIOClient={socket} />;
  return <AnalyticsPageComponent />;
};

export default AdminAnalyticsPage;
